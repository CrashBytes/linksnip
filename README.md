# LinkSnip

A tiny URL shortener — built end-to-end by [Pipemason](https://pipemason.com) from a single plain-English prompt.

See the build: the opening PR on this repo is the entire app, contract-first and test-driven, produced by one `pipemason start` run.
